"use strict"; // after using this word all the code will be treated as newer version

// alert(4+4)  // we are using nodeJS, not Browser

console.log(3 + 3)
console.log("Akash")

let name = "Akash" //string
let age = 19 //number, we also use bigInt for numbers greater than 2 to the power 53
let is_logged_in = true //boolean
let value = null

// null - this is a standalon value(datatype of null is object) (it is a representation of empty value)
// undefined - abhi value assign nahi huyi hai
// symbol - unique cheeje k bare me baat karne ke liye use hota hai
// object

console.log(typeof null); // gives output - object 
console.log(typeof undefined); //gives output - undefined